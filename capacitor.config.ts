import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uk.ey.timesheet-helper.app',
  appName: 'timesheet-helper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
